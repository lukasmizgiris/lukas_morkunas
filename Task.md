# Task

Implement purchase of an `Item`.

Requirements:

- `Order` must be created on `Item` purchase
  - `Payment` must be created in FakePay System
- There must be a separate endpoint to confirm order payment and update state to PAID.
